name: Docker Build & Publish

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"

      - name: Build Docker Image
        run: docker build -t peytondenisse/nature-api:${{ github.sha }} .

      - name: Tag Latest
        run: docker tag peytondenisse/nature-api:${{ github.sha }} peytondenisse/nature-api:latest

      - name: Push Docker Image
        run: |
          docker push peytondenisse/nature-api:${{ github.sha }}
          docker push peytondenisse/nature-api:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to AWS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            docker system prune -af --volumes
            df -h
            docker pull peytondenisse/nature-api:latest
            cd containers
            docker compose down
            docker compose up -d --force-recreate
            